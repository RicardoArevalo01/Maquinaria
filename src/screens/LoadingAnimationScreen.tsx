import { Text, View } from "react-native"

export const LoadingAnimationScreen = (  ) => {
    return (

        <View>
            <Text>
                Loading Animation Screen
            </Text>
        </View>

    )
}