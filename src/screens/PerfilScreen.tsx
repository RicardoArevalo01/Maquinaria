
import { HeaderTitle, ScreenContainer } from "../components/shared"

export const PerfilScreen = (  ) => {
    return (

        <ScreenContainer>
            <HeaderTitle 
                text="Perfil"
            />
        </ScreenContainer>

    )
}